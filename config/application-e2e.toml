# Used by the Playwright tests in e2e/ (MICRONAUT_ENVIRONMENTS=e2e): random port, throw-away database.
[micronaut.server]
port = -1

[datasources.default]
url = "jdbc:h2:mem:calendar-e2e;DB_CLOSE_DELAY=-1"

[calendar]
reminder-check-interval = "2s"
